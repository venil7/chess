import Move from './move';
import Board from './board';
import { Color } from './pieces/index';

class Game {
  public minimax(board: Board, color: Color, move: Move, depth: number = 0): Move {

  }
}